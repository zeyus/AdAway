apply plugin: 'com.android.library'

android {
    compileSdkVersion 28

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 28
    }

    // Do not abort build if lint finds errors
    namespace 'org.sufficientlysecure.rootcommands'
    lint {
        abortOnError false
    }
}

dependencies {
}
